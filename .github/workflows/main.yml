

on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
        uses: actions/checkout@v2

        name: "Login"
        run: docker login --username rahmaismaill6 --password dckr_pat_dW9nnjQNnRpj3R0FiYG_maPV4D0